# Moltbot Request

Source issue: https://github.com/anboas/Whitepaper/issues/20

Repo: Whitepaper

Request:
Improve reliability of Moltbot autonomy runs:

1) Stabilize secret loading:
- Stop depending on Vaultwarden/bw in cron (it can hang / flake).
- Prefer local secret file `/home/anboas/.secrets/openai_api_key` for cron runs.
- Keep Vaultwarden fetch as optional/manual or fallback.

2) Improve "why skipped" logging:
- When issue intake finds issues but doesn’t open PR, log why.
- When autonomy finds PRs but doesn’t act, log why (e.g., missing key, not single-paper PR, no request block, merge not ready, checks pending, etc.).
- Write these to `logs/moltbot-autonomy/runner.log` and include a short PR comment when applicable.

Constraints:
- No secrets in git.
- Prefer simple, robust behavior over clever.
